{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "test_atcoder_sample",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "type": "shell",
            "command": "ojtest",
            "args": [
                "${fileBasename}"
            ],
            "options": {
                "cwd": "${fileDirname}",
            }
        }
    ]
}
